return {
    {
        "github/copilot.vim",
        cmd = "Copilot",   -- lazy-load when first running the :Copilot... command 
        event = "InsertEnter", -- or lazy-load on first insert
    },
}
